#!/usr/bin/env ruby
$LOAD_PATH.unshift "#{__dir__}/../lib"
require 'boom'

if $0==__FILE__
  begin
    Evaluator.run(ARGF.read)
  rescue TypeInference::InferenceError => ex
    warn "[InferenceError] #{ex.message}"
  end
end
